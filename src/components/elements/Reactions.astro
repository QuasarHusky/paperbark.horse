---
interface Props {
    settings?: any;
    iid: string;
}

const props = Astro.props;

let settingsVal;

if (props.settings) {
    settingsVal = encodeURIComponent(JSON.stringify(props.settings));
}
---

<ws-widget type="reactions" iid={props.iid} settings={settingsVal}></ws-widget>
